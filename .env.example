SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=gokulgokul077g@gmail.com
SMTP_PASS=98589trgrnjhjrhjrfhjjrhneefrfgq
ORDER_EMAIL=gokulgokul077g@gmail.com
EOF
# Telegram order notifications — see docs/telegram-notifications.md
# Token from @BotFather. Chat ID from: npm run telegram:chat-id
# One chat, or several separated by commas.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Google Analytics 4 — Measurement ID from GA4 -> Admin -> Data Streams (G-XXXXXXXXXX).
# Leave blank to disable GA4. NEXT_PUBLIC_ vars are inlined at BUILD time, so set
# it in the build environment (e.g. Vercel env vars), not just at runtime.
# Do NOT also add a GA4 tag inside GTM (GTM-PRR9VW2H) or hits are counted twice.
NEXT_PUBLIC_GA_ID=
